spring:
  application:
    name: test
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: siva
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
    show-sql: true
